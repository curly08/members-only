<div class="content">
  <h1 class="title"><%= @post.title %></h1>
  <p class="subtitle"><%= @post.body %></p>
</div>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %> |
<%= link_to 'Delete', post_path, method: :delete, data: { confirm: "Are you sure you want to delete this tweeet?" } %>
